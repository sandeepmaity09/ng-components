import { BgchangerDirective } from './bgchanger.directive';

describe('BgchangerDirective', () => {
  it('should create an instance', () => {
    const directive = new BgchangerDirective();
    expect(directive).toBeTruthy();
  });
});
